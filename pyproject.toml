[tool.ruff]
line-length = 88
target-version = "py311"

[tool.ruff.lint]
select = ["E", "F", "W", "I"]  # ошибки, неиспользуемые импорты, warnings, сортировка импортов
ignore = ["E501"]  # игнорируем длинные строки (black сам форматирует)
